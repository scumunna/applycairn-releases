# Jobs

ApplyCairn creates one folder per assessed job. Each application packet has immutable versioned Markdown, DOCX, PDF, and a manifest containing current evidence/profile/job bindings, source mappings, artifact hashes, extracted-text hashes, and measured page counts. A newer packet never inherits old reviews.
